'use strict'
let numberX;
console.log('If a variable is declared, but a value haven\'t been assigned yet, then the value by default is undefined.');
console.log(numberX);
numberX = 7;
console.log('Now I initialized the variable numberX and its value is integer 7.');
console.log(numberX);