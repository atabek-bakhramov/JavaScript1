'use strict'
const calculateDogAge = (dogAge) => {
    const convertedAge = dogAge * 7;
    console.log(`Your doggie is ${convertedAge} years old in dog years!`);
};

calculateDogAge(1);
calculateDogAge(2);
calculateDogAge(3);